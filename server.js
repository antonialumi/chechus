const pasa = false;

function solicitarAccesoDesdeElServidor(nombre,password,email){
    if(pasa){
        // devuelve caso exitoso
        return "exito"
    }
    // devuelve error generico
    return "error"
}